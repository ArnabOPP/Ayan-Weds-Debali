# Framer React Bundle Recovery

## What was recovered
- The downloaded site is a **compiled React app generated by Framer**.
- All `.mjs` chunks referenced by `City5_files/script_main.DJTN5KON.mjs` are now present locally in `City5_files/`.
- `City5.html` is already configured to use local module files (`./City5_files/*.mjs`).

## Important limitation
- This is **not the original editable React source code** (no original JSX components, source folder structure, or Framer project files).
- It is the production bundle, restored so it can run locally/offline.

## Run locally
Use a local HTTP server (do not open via `file://`):

```powershell
cd C:\Users\VICTUS\Desktop\ffh
# any static server is fine, examples:
# npx serve .
# or use your preferred VS Code Live Server extension
```

Then open `City5.html` through that local server URL.

## Re-run recovery (if needed)
Script available at:
- `recover_framer_bundle.ps1`

It can download Framer modules and rewrite `City5.html` to local paths when CDN links are present.
